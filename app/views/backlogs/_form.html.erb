<%= error_messages_for :backlog %>
	
<% form_for(@backlog) do |f| %>
  <p>
    <b>Nome</b><br />
    <%= f.text_field :name %>
  </p>

  <p>
    <b>Comentário</b><br />
    <%= f.text_area :comment %>
  </p>

  <p>
    <%= f.submit "Salvar" %>
  </p>
<% end %>

<% content_for :side do %>
	<%= render_formatting_help %>
<% end %>
