<%= error_messages_for :sprint %>

<% form_for(@sprint) do |f| %>

	<p>
	  <b>Product Backlog</b><br />
	  <%= f.select :backlog_id, @backlogs.collect { |b| [b.name, b.id] }, :prompt => "-- Selecione --" %>
	</p>

  <p>
    <b>Comentário</b><br />
    <%= f.text_area :comments, :rows => 5 %>
  </p>

  <p>
    <%= f.submit "Salvar" %>
  </p>
<% end %>

<% content_for :side do %>
	<%= render_formatting_help %>
<% end %>